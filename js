

> filter : var new = array.filter(function)
	> filter passes each element of an array through a function
		> if the function returns a turthy value then the element is kept other wise not

> JSON.stringify(data) : used to turn a javascript object into JSON.

> JSON.parse(data) : used to turn JSON into a javascript object

